# Questions

Questions to answer while learning.

- What exactly is a tensor stride?
- How does broadcasting affect gradients?
- What does PyTorch store for backward computation?
- When does `.detach()` matter?
- What operations are expensive on CPU vs GPU/MPS?
- How do small autograd engines represent operations differently from PyTorch?
